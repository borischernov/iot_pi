<h1>Settings</h1>
<%= form("/settings", :post) %>
	<%= erb :'settings/_settings', locals: {settings: @settings, all_settings: @all_settings, prefix: 'settings'} %>
<button type="submit" class="btn btn-primary">Save</button>
<a href='/' class="btn btn-secondary">Cancel</a>
</form>
